package com.example.coreservice.enums;

public enum CommentStatus {
    ACTIVE,    // Đang hiển thị
    RESOLVED,  // Đã giải quyết (giống Google Docs, ẩn đi nhưng vẫn lưu trong history)
    DELETED    // Đã xóa
}
